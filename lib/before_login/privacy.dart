import 'package:flutter/material.dart';

class PrivacyCurrency extends StatelessWidget {
  const PrivacyCurrency({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("priv");
  }
}
